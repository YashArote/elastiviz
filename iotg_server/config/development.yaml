# This is the configuration file for your local development environment. By
# default, it runs a single server on port 8080. To set up your server, you will
# need to add the name of the database you are connecting to and the user name.
# The password for the database is stored in the config/passwords.yaml.
#
# When running your server locally, the server ports are the same as the public
# facing ports.

# Configuration for the main API server.
apiServer:
  port: 8080
  publicHost: 192.168.0.106
  publicPort: 8080
  publicScheme: http

# Configuration for the Insights server.
insightsServer:
  port: 8081
  publicHost: 192.168.0.106
  publicPort: 8081
  publicScheme: http

# Configuration for the web server.
webServer:
  port: 8082
  publicHost: 192.168.0.106
  publicPort: 8082
  publicScheme: http

# This is the database setup for your server.
database:
  host: localhost
  port: 8090
  name: iotg
  user: postgres
  #requireSsl: true # defaults to false
  #isUnixSocket: true # defaults to false
  #maxConnectionCount: 10 # Defaults to 10, a negative or null value removes the limit

# This is the setup for Redis.
redis:
  enabled: false
  host: localhost
  port: 8091
  #user: # defaults to empty
  #requireSsl: true # defaults to false

maxRequestSize: 524288 # The maximum size of requests allowed in bytes

# Elasticsearch Cloud (Serverless) configuration
# Replace the placeholder values with your actual Elastic Cloud endpoint.
elasticsearch:
  url: 'https://my-observability-project-cb9415.es.us-central1.gcp.elastic.cloud'
  # API key is stored in passwords.yaml under key: elasticsearchApiKey

# LLM configuration (Gemini)
llm:
  provider: gemini
  model: gemini-1.5-flash
  # API key is stored in passwords.yaml under key: geminiApiKey

# Schema ingestion settings
schemaIngestor:
  refreshIntervalMinutes: 15

# Elastic AI Agent Builder (Serverless Elasticsearch project)
# Create an inference endpoint in Kibana → Stack Management → ML → Inference Endpoints
# Then set the inference ID here (e.g. "my-observability-agent")
elastic:
  # Kibana URL for the Serverless Elasticsearch project
  kibanaUrl: 'https://iotg-b3fc4e.kb.us-central1.gcp.elastic.cloud'
  # Agent ID — set this after creating your agent in Kibana Agent Builder UI
  agentId: 'iotg-observability-agent'
  # Public URL where Serverpod is accessible (for Agent Builder tool webhooks)
  # Use ngrok or your deployed URL: e.g. https://abc123.ngrok.io
  toolWebhookBaseUrl: 'http://localhost:8080'

sessionLogs:
  persistentEnabled: true
  consoleEnabled: true
  consoleLogFormat: text # Defaults to "json", options are "text" or "json"

# futureCallExecutionEnabled: true # Defaults to true

#futureCall:
#  concurrencyLimit: 1 # Defaults to 1, a negative or null value removes the limit
#  scanInterval: 5000 # Unit in milliseconds, defaults to 5000
